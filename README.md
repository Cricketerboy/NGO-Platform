# Steps to run this project
* Clone this repo using  **git clone**
  
**Backend**
* Install dependencis using **npm install**
* Add .env with the **DATABASE_URL**
* Run this using **npm run dev**

**Frontend**
* Install dependencis using **npm install**
* Run this using **npm run dev**
